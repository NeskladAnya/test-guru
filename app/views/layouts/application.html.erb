<!DOCTYPE html>
<html>
  <head>
    <title>TestGuru</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p><%= project_link %></p>
    <% if logged_in? %>
      <p class="nav user">
        Welcome, <b><%= current_user.name %>! <%= link_to "Log out", logout_path, method: :delete %></b>
      </p>
    <% else %>
      <p class="nav user">
        <b><%= link_to "Log in", login_path %> || <%= link_to "Sign up", signup_path%></b>
      </p>
    <% end %>

    <%= yield %>

    <%= render 'shared/footer' %>
  </body>
</html>
