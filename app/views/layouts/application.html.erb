<!DOCTYPE html>
<html>
  <head>
    <title>TestGuru</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p><%= project_link %></p>
    
    <% if user_signed_in? %>
      <p class="nav user">
        Welcome, <b><%= current_user.first_name %>! <%= link_to "Log out", destroy_user_session_path, method: :delete %></b>
      </p>
    <% else %>
      <p class="nav user">
        <b><%= link_to "Log in", new_user_session_path %> || <%= link_to "Sign up", new_user_registration_path %></b>
      </p>
    <% end %>

    <%= render 'shared/flash' %>

    <%= yield %>

    <%= render 'shared/footer' %>
  </body>
</html>
