<div class="row">
  <h1><%= t('.header') %></h1>

  <table class="table table-hover table-bordered">
    <thead>
      <tr>
        <th class="col text-center"><%= t('.question') %></th>
        <th class="col text-center"><%= t('.url') %></th>
        <th class="col text-center"><%= t('.author') %></th>
      </tr>
    </thead>

    <tbody>
      <%= render @gists%>
    </tbody>
  </table>
</div>
